<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oocl.web.mapper.PermissionMapper">

    <insert id="addPermission" parameterType="permission">
        insert into permission(permissionId, permissionName, permissionDescription) values(#{permissionId}, #{permissionName}, #{permissionDescription})
    </insert>
</mapper>